from setuptools import setup

setup(name='frew_env',
    version='0.1',
    install_requires=['gym',
                      'pywin32',
                      'numpy',
                      'keras',
                      'tensorflow',
                      'pandas',
                      'matplotlib'
    ]
)